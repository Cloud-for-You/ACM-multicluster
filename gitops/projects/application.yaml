---
apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: projects
spec:
  selector:
    matchLabels:
      app: project
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
