---
apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: minio-tenant
spec:
  selector:
    matchLabels:
      app: minio-tenant
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
