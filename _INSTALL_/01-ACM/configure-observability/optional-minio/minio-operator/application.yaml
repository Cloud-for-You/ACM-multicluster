---
apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: minio-operator
spec:
  selector:
    matchLabels:
      app: minio-operator
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
